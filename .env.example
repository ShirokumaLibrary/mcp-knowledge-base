# Example environment configuration
# Copy this file to .env and update with your values

# IMPORTANT: Use ABSOLUTE PATHS (full paths) for all file/directory configurations
# Relative paths will cause issues with Prisma migrations and other tools

# Prisma database connection URL (MUST be absolute path)
# Replace /path/to/your/project with your actual project path
SHIROKUMA_DATABASE_URL=file:/path/to/your/project/.shirokuma/data/shirokuma.db

# Data directory path (MUST be absolute path)
# Replace /path/to/your/project with your actual project path
SHIROKUMA_DATA_DIR=/path/to/your/project/.shirokuma/data

# Export directory path (MUST be absolute path)
# Replace /path/to/your/project with your actual project path
SHIROKUMA_EXPORT_DIR=/path/to/your/project/docs/export

# Claude API key (optional)
# ANTHROPIC_API_KEY=your-secret-key-here

# Environment type
# Values: development, production, test
NODE_ENV=development

# Note: You can generate a properly configured .env file using:
# shirokuma-kb config export > .env

