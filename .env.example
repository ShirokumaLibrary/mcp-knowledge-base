# Example environment configuration
# Copy this file to .env and update with your values

# Data directory path (defaults to .shirokuma/data)
SHIROKUMA_DATA_DIR=/path/to/project/.shirokuma/data

# Export directory path
SHIROKUMA_EXPORT_DIR=/path/to/project/docs/export

